import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "No Dogs Allowed - Official Wiki",
  description: "The complete official wiki and documentation for No Dogs Allowed by Siphon Games. Learn about mechanics, lore, characters, combat, locations, and everything else about the game.",
  authors: [{ name: "Siphon Games" }],
  keywords: ["No Dogs Allowed", "Siphon Games", "Wiki", "Documentation", "Game Guide"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
